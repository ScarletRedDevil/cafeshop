<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
<mapper namespace="com.sds.cafeshop.model.order.OrderStatDAO">
	<select id="select" resultType="Paymethod">
		select * from orderstat
		where orderstat_idx = 1
	</select>
</mapper>